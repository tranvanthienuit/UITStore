using SQLite;
using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
using UITStore.Models;

namespace UITStore.ViewModels
{
    public class CartVM
    {
        /* private readonly string folder = Environment.GetFolderPath(Environment.SpecialFolder.Personal);
         public bool CreateDatabaseCart()
         {
             try
             {
                 var connection = new SQLiteConnection
                     (Path.Combine(folder, "uitStore.db"));
                 connection.CreateTable<Cart>();
                 return true;
             }
             catch
             {
                 return false;
             }
         }*/
        
    }
}
